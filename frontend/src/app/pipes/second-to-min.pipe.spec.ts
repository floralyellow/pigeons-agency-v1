import { SecondToMinPipe } from './second-to-min.pipe';

describe('SecondToMinPipe', () => {
  it('create an instance', () => {
    const pipe = new SecondToMinPipe();
    expect(pipe).toBeTruthy();
  });
});
