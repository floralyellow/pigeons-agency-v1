import { AviarySortPipe } from './aviary-sort.pipe';

describe('AviarySortPipe', () => {
  it('create an instance', () => {
    const pipe = new AviarySortPipe();
    expect(pipe).toBeTruthy();
  });
});
