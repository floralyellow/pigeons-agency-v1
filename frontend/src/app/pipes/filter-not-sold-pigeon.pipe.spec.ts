import { FilterNotSoldPigeonPipe } from './filter-not-sold-pigeon.pipe';

describe('FilterNotSoldPigeonPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterNotSoldPigeonPipe();
    expect(pipe).toBeTruthy();
  });
});
